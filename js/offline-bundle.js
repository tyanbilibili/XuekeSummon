/* AUTO-GENERATED by tools/build-offline.js -- do not edit */
(function () {
  var __src = {"../data/characters":"// 角色定义（9位学科角色）\n// element: '文' | '理'   body: '大' | '小'\nmodule.exports = {\n  yuwen: {\n    id: 'yuwen', name: '语文', short: '语', element: '文', body: '大', maxHp: 15, chargeNeed: 3,\n    img: '/assets/characters/语文.png',\n    a: { name: '普通攻击', cost: 2, el: '物理', dmg: 2, desc: '造成2点物理伤害' },\n    s: { name: '元素战技', cost: 3, el: '文', dmg: 2, summon: 'zuowen', desc: '造成2点文元素伤害，生成「作文」（回合结束造成1点文伤害，持续2回合）' },\n    b: { name: '元素爆发', cost: 5, charge: 3, el: '文', dmg: 4, dmg2: 3, desc: '造成4点文元素伤害和3点后台穿透伤害，施加50%易伤（持续2回合，可叠加2次）；后台穿透伤害不享受易伤' },\n    talent: {\n      name: '薪火相传', desc: '偶数回合结束后，可向对方提问一个学过的知识点，若对方10秒内未答出，则下一回合内最多3次「语文」的元素战技和元素爆发所需行动点-1且你获得先手（双方都有语文则抵消，双方先后手效果互相抵消，仍按先结束的一方为先手）',\n      type: 'question'\n    }\n  },\n  shuxue: {\n    id: 'shuxue', name: '数学', short: '数', element: '理', body: '大', maxHp: 15, chargeNeed: 3,\n    img: '/assets/characters/数学.png',\n    a: { name: '普通攻击', cost: 2, el: '理', dmg: 1, desc: '造成1点理元素伤害' },\n    s: { name: '元素战技', cost: 4, el: '理', dmg: 1, summon: 'jiexijihe', desc: '造成1点理元素伤害，生成「解析几何」（回合结束造成1点理伤害，持续2回合）' },\n    b: { name: '元素爆发', cost: 4, charge: 3, el: '理', dmg: 0, desc: '对对方角色施加只能使用1次150%的易伤' },\n    talent: {\n      name: '数学天赋', desc: '从数学切换至其他角色时，视为快速行动（每回合仅限1次）',\n      type: 'fast_switch'\n    }\n  },\n  yingyu: {\n    id: 'yingyu', name: '英语', short: '英', element: '文', body: '大', maxHp: 15, chargeNeed: 3,\n    img: '/assets/characters/英语.png',\n    a: { name: '普通攻击', cost: 2, el: '物理', dmg: 2, desc: '造成2点物理伤害' },\n    s: { name: '元素战技', cost: 3, el: '文', dmg: 0, infuse: true, desc: '使普通攻击获得文元素附魔且伤害+1，持续2回合（元素战技无法增加充能）' },\n    b: { name: '元素爆发', cost: 6, charge: 3, el: '文', dmg: 7, camo: true, desc: '造成7点文元素伤害，自身获得持续2回合的「迷彩」（对方无法对该角色造成伤害，召唤物伤害不受影响）' },\n    talent: {\n      name: '英语天赋', desc: '若由英语角色产生了元素反应，行动点+1（每回合仅限1次）',\n      type: 'reaction_ap'\n    }\n  },\n  wuli: {\n    id: 'wuli', name: '物理', short: '物', element: '理', body: '小', maxHp: 12, chargeNeed: 3,\n    img: '/assets/characters/物理.png',\n    a: { name: '普通攻击', cost: 2, el: '物理', dmg: 2, desc: '造成2点物理伤害' },\n    s: { name: '元素战技', cost: 3, el: '理', dmg: 2, selfAttach: '理', desc: '造成2点理元素伤害，自身附着理元素' },\n    b: { name: '元素爆发', cost: 4, charge: 3, el: '理', dmg: 1, summon: 'minglang', desc: '造成1点理元素伤害，生成「明朗之气」（我方角色攻击时协同造成2点理伤害，可用3次）' },\n    talent: {\n      name: '物理天赋', desc: '每回合结束后，移除自身附着的元素',\n      type: 'clear_element_end'\n    }\n  },\n  huaxue: {\n    id: 'huaxue', name: '化学', short: '化', element: '理', body: '小', maxHp: 12, chargeNeed: 3,\n    img: '/assets/characters/化学.png',\n    a: { name: '普通攻击', cost: 2, el: '理', dmg: 1, desc: '造成1点理元素伤害' },\n    s: { name: '元素战技', cost: 4, el: '理', dmg: 2, allEnemy: true, selfHeal: 2, desc: '对对方所有角色造成2点理元素伤害，自身+2血（每回合限用1次）' },\n    b: { name: '元素爆发', cost: 5, charge: 3, el: '理', dmg: 2, dmg2: 2, stun: true, desc: '造成2点理元素伤害和2点后台穿透伤害，对方获得持续2回合的「眩晕」（每个行动轮结束时强制其切换为左侧角色）' },\n    talent: {\n      name: '化学天赋', desc: '使用元素战技时，自身+2血',\n      type: 'skill_selfheal'\n    }\n  },\n  shengwu: {\n    id: 'shengwu', name: '生物', short: '生', element: '理', body: '小', maxHp: 12, chargeNeed: 3,\n    img: '/assets/characters/生物.png',\n    a: { name: '普通攻击', cost: 2, el: '理', dmg: 2, desc: '造成2点理元素伤害' },\n    s: { name: '元素战技·一', cost: 3, el: '理', dmg: 0, summon: 'qixing', n: 1, dur: 2, desc: '生成1个「奇行种」（回合结束造成1点理伤害，持续2回合）' },\n    s2: { name: '元素战技·二', cost: 7, el: '理', dmg: 0, summon: 'qixing', n: 2, dur: 3, desc: '生成2个「奇行种」（回合结束造成1点理伤害，持续3回合）' },\n    b: { name: '元素爆发', cost: 6, charge: 3, el: '理', dmg: 5, summonScale: true, healTeam: 4, desc: '造成5点理元素伤害，每存在1个召唤物使此伤害+1；回复全队角色4点血量' },\n    talent: {\n      name: '生物天赋', desc: '回合结束时（奇行种生效后计数）每存在1个奇行种，下回合行动点+1（最多+2）；若存在3个或以上奇行种，则下回合一定是你先手',\n      type: 'summon_ap'\n    }\n  },\n  zhengzhi: {\n    id: 'zhengzhi', name: '政治', short: '政', element: '文', body: '小', maxHp: 12, chargeNeed: 3,\n    img: '/assets/characters/政治.png',\n    a: { name: '普通攻击', cost: 3, el: '物理', dmg: 3, shield: 1, desc: '造成3点物理伤害，生成1层护盾（最多叠4层）' },\n    s: { name: '元素战技', cost: 3, el: '文', dmg: 2, shield: 1, huxinkai: true, desc: '造成2点文元素伤害，获得1层护盾和「护心铠」（受到3点以上伤害时抵消2点；不可叠加，可用2次）' },\n    b: { name: '元素爆发', cost: 2, charge: 3, el: '文', dmg: 0, politBuff: true, desc: '普通攻击获得文元素附魔且伤害+1并少消耗1点行动点，持续2回合；期间获得护盾量翻倍' },\n    talent: {\n      name: '政治天赋', desc: '政治角色身上存在护盾时，造成的伤害-1，但受到的伤害-1',\n      type: 'shield_tradeoff'\n    }\n  },\n  lishi: {\n    id: 'lishi', name: '历史', short: '史', element: '文', body: '小', maxHp: 12, chargeNeed: 3,\n    img: '/assets/characters/历史.png',\n    a: { name: '普通攻击', cost: 2, el: '文', dmg: 1, desc: '造成1点文元素伤害' },\n    s: { name: '元素战技', cost: 3, el: '文', dmg: 2, healTeam: 1, desc: '造成2点文元素伤害，治疗全队角色1点血量' },\n    b: { name: '元素爆发', cost: 6, charge: 3, el: '文', dmg: 3, dmg2: 6, shield: 2, desc: '造成3点文元素伤害和6点后台穿透伤害，获得2层护盾' },\n    talent: {\n      name: '历史天赋', desc: '己方角色对对方处在护盾下的角色造成的伤害+30%',\n      type: 'shield_bonus'\n    }\n  },\n  dili: {\n    id: 'dili', name: '地理', short: '地', element: '文', body: '小', maxHp: 12, chargeNeed: 3,\n    img: '/assets/characters/地理.png',\n    a: { name: '普通攻击', cost: 2, el: '物理', dmg: 2, desc: '造成2点物理伤害' },\n    s: { name: '元素战技', cost: 4, el: '文', dmg: 3, vulnerable: 20, desc: '造成3点文元素伤害，对对方该名角色施加20%易伤（可无限叠加，持续2回合，各持续分开计算）' },\n    b: { name: '元素爆发', cost: 5, charge: 3, el: '理', dmg: 2, allEnemy: true, leibao: true, desc: '对对方所有角色造成2点理元素伤害，获得「雷暴之法」（己方角色造成伤害时协同造成3点文伤害，持续2回合）' },\n    talent: {\n      name: '地理天赋', desc: '可选择让对方轮空1个行动轮，己方行动点+2（每2回合仅限1次）',\n      type: 'skip_turn'\n    }\n  }\n};\n","../data/cards":"// 58 种手牌元数据。效果逻辑在 game/engine.js 中按 id 实现。\n// type: weapon | artifact | talent | resonance | scene | support | food\n// fast: true 表示打出后不结束你的行动轮（快速行动），默认为作战行动（结束行动轮）\nconst C = [\n  // ---------- 武器牌 ----------\n  { id:'w_caozhi', name:'草纸', type:'weapon', cost:1, el:'理', fast:false,\n    text:'装备者造成的伤害+1（每回合仅限1次，仅限理属性角色可装备）' },\n  { id:'w_caogaozhi', name:'草稿纸', type:'weapon', cost:2, el:'理', fast:false,\n    text:'装备者造成的伤害+1；当其触发元素反应时，行动点+2（每回合仅限1次，仅限理属性角色可装备）' },\n  { id:'w_jisuanji', name:'计算机', type:'weapon', cost:4, el:'理', fast:false,\n    text:'装备者造成的伤害+1；对方攻击装备者时，将受到伤害的75%反弹并回复受到伤害50%的血量（回血遵循四舍五入，仅限理属性角色可装备）' },\n  { id:'w_qianbi', name:'铅笔', type:'weapon', cost:1, el:'文', fast:false,\n    text:'装备者造成的伤害+1（每回合仅限1次，仅限文属性角色可装备）' },\n  { id:'w_heibi', name:'黑笔', type:'weapon', cost:2, el:'文', fast:false,\n    text:'装备者造成的伤害+1；当其触发元素反应时，下次切人视为快速行动（每回合仅限1次，仅限文属性角色可装备）' },\n  { id:'w_gangbi', name:'钢笔', type:'weapon', cost:4, el:'文', fast:false,\n    text:'装备者造成的伤害+1；每回合前双方各掷骰一次，若对方>己方则你当前出战角色+1充能；若对方<己方则对方本回合行动点-1；相同则无事发生（仅限文属性角色可装备）' },\n  { id:'w_renshu', name:'忍术作弊小抄', type:'weapon', cost:5, el:'文', fast:false,\n    text:'装备者造成的伤害+1；装备者释放元素爆发时，为全队角色回复1点充能（整局仅限2次，仅限文属性角色可装备）' },\n  { id:'w_shuzi', name:'数字奥法之卷', type:'weapon', cost:5, el:'理', fast:false,\n    text:'装备者造成的伤害+1；每回合前，可向对方提问1道24×24以内四则运算，若对方30秒内未答出，则你的下一次攻击视为快速行动；若答出则你本轮行动点+1（仅限理属性角色可装备）' },\n  { id:'w_huanyu', name:'寰宇支配之剑', type:'weapon', cost:5, el:null, fast:false,\n    text:'装备者造成的伤害+1；装备者受到的伤害+1；当装备者血量低于8点时，变为“伤害+3，受到伤害-1”；每个行动轮后获得1层护盾（不可叠加，持续1回合）' },\n\n  // ---------- 饰品牌 ----------\n  { id:'a_dutu', name:'赌徒的胸花', type:'artifact', cost:2, el:null, fast:false,\n    text:'对方每有1个角色死亡后：行动点+3，获得1点充能' },\n  { id:'a_hairan', name:'海染砗磲', type:'artifact', cost:3, el:null, fast:false,\n    text:'治疗加成+50%；溢出的治疗量以其75%对对方造成对应元素伤害（每2回合仅限1次）' },\n  { id:'a_chixiao', name:'嗤笑之面', type:'artifact', cost:3, el:null, fast:false,\n    text:'装备者造成的伤害+1；使用元素战技后回复自身1点血量（每回合仅限2次）' },\n  { id:'a_huanbao', name:'环保之心', type:'artifact', cost:2, el:null, fast:false,\n    text:'装备者攻击时，少消耗1个行动点（每回合仅限2次）' },\n\n  // ---------- 天赋牌（仅当对应角色为出战角色时可打出） ----------\n  { id:'t_yuwen', name:'语文·薪火不灭', type:'talent', cost:3, char:'yuwen', fast:false,\n    text:'造成2点文元素伤害，生成「作文」；作文持续时间改为无限，且伤害变为2点' },\n  { id:'t_shuxue', name:'数学·速充', type:'talent', cost:2, char:'shuxue', fast:false,\n    text:'数学的充能需求改为2点' },\n  { id:'t_yingyu', name:'英语·幽夜净土', type:'talent', cost:3, char:'yingyu', fast:false,\n    text:'英语释放元素战技后，获得2行动轮的迷彩buff' },\n  { id:'t_wuli', name:'物理·电压plus ultra', type:'talent', cost:3, char:'wuli', fast:false,\n    text:'物理触发元素反应时，该次伤害+1并造成1点后台穿透伤害且行动点+1（每回合仅限1次）' },\n  { id:'t_huaxue', name:'化学·扭转万象', type:'talent', cost:3, char:'huaxue', fast:false,\n    text:'眩晕debuff的切人方向可自定，且持续回合变为3回合' },\n  { id:'t_shengwu', name:'生物·三人成虎', type:'talent', cost:3, char:'shengwu', fast:false,\n    text:'每个「奇行种」在每回合开始前会给我方出战角色1层护盾' },\n  { id:'t_zhengzhi', name:'政治·智慧之论', type:'talent', cost:3, char:'zhengzhi', fast:false,\n    text:'按照政治护盾量的50%增加政治角色造成的伤害' },\n  { id:'t_lishi', name:'历史·巧思妙论', type:'talent', cost:2, char:'lishi', fast:false,\n    text:'可消耗1行动点，增加我方1名角色的1点充能（每2回合仅限1次）' },\n  { id:'t_dili', name:'地理·翻涌不息', type:'talent', cost:3, char:'dili', fast:false,\n    text:'可选择让对方轮空1个行动轮，己方行动点+2（每2回合仅限1次）' },\n\n  // ---------- 共鸣牌（当队伍中存在2名或更多该属性角色才可打出） ----------\n  { id:'r_li', name:'理·生生不息', type:'resonance', cost:0, el:'理', fast:true,\n    text:'行动点+2' },\n  { id:'r_wen', name:'文·生生不息', type:'resonance', cost:0, el:'文', fast:true,\n    text:'行动点+2' },\n  { id:'r_da', name:'大·以大博小', type:'resonance', cost:1, el:null, fast:true,\n    text:'大体型角色造成理元素反应时，伤害+2；造成文元素反应时，伤害+1' },\n  { id:'r_xiao', name:'小·以小博大', type:'resonance', cost:1, el:null, fast:true,\n    text:'小体型角色造成文元素反应时，伤害+2；造成理元素反应时，伤害+1' },\n\n  // ---------- 场景牌 ----------\n  { id:'s_jiaoshi', name:'教室', type:'scene', cost:2, fast:false,\n    text:'每回合结束后，回复血量最低的后台角色2点血量（持续2回合）' },\n  { id:'s_bangong', name:'教师办公室', type:'scene', cost:2, fast:false,\n    text:'每回合结束后，回复出战角色2点血量（持续2回合）' },\n  { id:'s_cesuo', name:'厕所', type:'scene', cost:1, fast:false,\n    text:'每回合开始前，掷骰次数+1，取较好的一次计算行动点数' },\n  { id:'s_jia', name:'家', type:'scene', cost:2, fast:false,\n    text:'我方所有角色血量和血量上限+1' },\n  { id:'s_shitang', name:'食堂', type:'scene', cost:3, fast:false,\n    text:'每回合可打出对方角色的元素战技1次（不增加充能，持续3回合）' },\n  { id:'s_tiyuguan', name:'体育馆', type:'scene', cost:2, fast:false,\n    text:'每回合第1次切人时视为快速行动' },\n  { id:'s_caochang', name:'操场', type:'scene', cost:2, fast:false,\n    text:'每回合结束后，抽2张牌（持续2回合）' },\n  { id:'s_yiwushi', name:'医务室', type:'scene', cost:2, fast:false,\n    text:'每回合第1次切人时，少花费1行动点' },\n  { id:'s_xiaomaibu', name:'小卖部', type:'scene', cost:1, fast:false,\n    text:'本回合及下一回合行动点-1，但再下一回合行动点+4，随后弃置此牌' },\n\n  // ---------- 支援牌 ----------\n  { id:'sup_zuoye', name:'作业答案', type:'support', cost:3, fast:false,\n    text:'每回合开始前，行动点+4（持续2回合）' },\n  { id:'sup_yingyang', name:'便携营养袋', type:'support', cost:2, fast:false,\n    text:'打出料理牌时无需消耗行动点（持续3回合）' },\n  { id:'sup_canliang', name:'参量变质仪', type:'support', cost:2, fast:false,\n    text:'双方共造成5次元素伤害后，此牌弃置，行动点+4' },\n  { id:'sup_shifu', name:'武器大师', type:'support', cost:2, fast:false,\n    text:'打出此牌2回合后弃置；此后打出武器牌无需消耗行动点（仅限1次）' },\n  { id:'sup_niunai', name:'牛奶', type:'support', cost:1, fast:true,\n    text:'选择1名角色，清除其附着的元素和异常状态（如应激反应、失忆反应）' },\n  { id:'sup_yunchou', name:'运筹帷幄', type:'support', cost:1, fast:true,\n    text:'抓2张牌' },\n  { id:'sup_jinji', name:'紧急战术', type:'support', cost:0, fast:false,\n    text:'我方下次切人时，视为快速行动' },\n  { id:'sup_chongdian', name:'充电宝', type:'support', cost:2, fast:true,\n    text:'我方出战角色获得1点充能' },\n  { id:'sup_dongban', name:'东搬西补', type:'support', cost:1, fast:true,\n    text:'从我方后台角色中，转移1点充能给出战角色（若后台角色无充能则无法打出）' },\n  { id:'sup_douzhuan', name:'斗转星移', type:'support', cost:0, fast:true,\n    text:'将我方1名角色所装备的武器牌或饰品牌转移到另一个可装备的角色身上' },\n  { id:'sup_yueye', name:'月夜刺猹图', type:'support', cost:4, fast:false,\n    text:'消灭对方所有召唤物；我方召唤物伤害+1（持续1回合）' },\n  { id:'sup_jingzhun', name:'精准打击', type:'support', cost:1, fast:false,\n    text:'下次攻击可自选对方1名角色挨打' },\n  { id:'sup_songni', name:'送你一程', type:'support', cost:2, fast:true,\n    text:'选择对方1个召唤物，将其消灭' },\n  { id:'sup_zhaohuanshi', name:'召唤师', type:'support', cost:2, fast:false,\n    text:'生成1个自选元素召唤物，回合结束时造成2点对应元素伤害（持续2回合）' },\n  { id:'sup_wohai', name:'我还能活', type:'support', cost:0, fast:true,\n    text:'行动点+2，我方出战角色充能+1（仅当我方该回合有角色死亡时可打出）' },\n  { id:'sup_chegu', name:'彻骨冰寒', type:'support', cost:2, fast:false,\n    text:'选择对方1名角色，使其无法行动并附上仅限1次的30%易伤（持续1回合）' },\n\n  // ---------- 料理牌（每回合每位角色只能食用1次料理） ----------\n  { id:'f_tiantianhua', name:'甜甜花酿鸡', type:'food', cost:0, fast:true,\n    text:'食用者回复1点血量' },\n  { id:'f_yanxun', name:'北地烟熏鸡', type:'food', cost:1, fast:true,\n    text:'食用者本回合内接下来3次普通攻击所需行动点-1' },\n  { id:'f_boluo', name:'菠萝披萨', type:'food', cost:1, fast:true,\n    text:'食用者回复2点血量；每回合结束后回复1点血量（持续2回合）' },\n  { id:'f_lianhua', name:'莲花酥', type:'food', cost:1, fast:true,\n    text:'本回合中，食用者接下来2次受到的伤害-3' },\n  { id:'f_xiantiao', name:'仙跳墙', type:'food', cost:2, fast:true,\n    text:'食用者本回合内元素爆发伤害+4（仅限1次）' },\n  { id:'f_jueyun', name:'绝云锅巴', type:'food', cost:0, fast:true,\n    text:'扣除食用者3点血量，行动点+6' },\n  { id:'f_badou', name:'巴豆', type:'food', cost:1, fast:true,\n    text:'食用者下次攻击时伤害+1；对方下次攻击造成的伤害-1但消耗的行动点-1' }\n];\n\nconst byId = {};\nC.forEach(c => { byId[c.id] = c; });\n\nconst IMG = {};\nC.forEach(c => { IMG[c.id] = '/assets/cards/' + c.name + '.png'; });\n\nmodule.exports = { cards: C, byId, IMG };\n","./engine":"// 《学科召唤》规则引擎（服务端权威）。纯逻辑，不含 IO。\nconst CHARS = require('../data/characters');\nconst { byId: CARD, cards: CARDS, IMG } = require('../data/cards');\n\nconst PREP = { physical: '物理', wen: '文', li: '理' };\n\nfunction createRng(seed) {\n  let s = (seed == null) ? Math.floor(Math.random() * 1e9) : seed >>> 0;\n  return function () {\n    s = (s * 1664525 + 1013904223) >>> 0;\n    return s / 4294967296;\n  };\n}\n\nfunction roundHalfUp(x) {\n  // 四舍五入\n  return Math.round(x + 1e-9);\n}\n\nfunction cloneObj(o) { return JSON.parse(JSON.stringify(o)); }\n\n// ---------------- 基础状态 ----------------\nfunction makeChar(id, maxHpOverride) {\n  const c = CHARS[id];\n  const maxHp = maxHpOverride != null ? maxHpOverride : c.maxHp;\n  return {\n    id, name: c.name, element: c.element, body: c.body,\n    a: c.a, s: c.s, s2: c.s2, b: c.b, chargeNeed: c.chargeNeed,\n    maxHp, hp: maxHp, charge: 0,\n    attached: null,           // 附着元素 '文'|'理'\n    weapon: null, artifact: null, talent: null,\n    shield: 0,                // 护盾\n    status: {},               // 各类状态\n    buffs: [],                // 附加状态数组 {key,dur,val,...}\n    bigHealUsed: 0\n  };\n}\n\nfunction makeSummon(side, key, dmg, el, dur, uses, ownerChar, endOfRound) {\n  return { side, key, dmg, el, dur, uses, owner: ownerChar || null, carry: 0, endOfRound: endOfRound !== false };\n}\n\nfunction createPlayer(side, teamIds, activeIndex, deckIds, opts) {\n  return {\n    side, name: opts?.name || ('玩家' + (side + 1)), isAI: !!opts?.isAI,\n    team: teamIds.map(id => makeChar(id)),\n    activeIndex, hand: [], deck: deckIds.map(id => ({ id })), discards: [],\n    summons: [],\n    supports: [],   // 支援牌 {id,dur,...}\n    scenes: [],     // 场景牌 {id,dur,...}\n    ap: 0, apUsed: 0,\n    weaponFree: false,\n    firstNextRound: false,\n    passed: false,\n    // 每回合结算的临时标记\n    roundFlags: {},\n    stats: { kills: 0 }\n  };\n}\n\nfunction createGame(settings) {\n  const rng = createRng(settings?.seed);\n  const p0 = createPlayer(0, settings.team0, settings.active0 != null ? settings.active0 : 0,\n    settings.deck0, { name: settings.name0, isAI: settings.ai0 });\n  const p1 = createPlayer(1, settings.team1, settings.active1 != null ? settings.active1 : 0,\n    settings.deck1, { name: settings.name1, isAI: settings.ai1 });\n  const state = {\n    rng,\n    players: [p0, p1],\n    round: 0,               // 开始前为 0，开始第一回合后为 1\n    phase: 'setup',         // setup | decideroll | round | finished\n    current: 0,             // 当前行动轮归属的玩家\n    firstThisRound: 0,\n    earlyEndedBy: null,     // 本回合最开始提前结束的玩家 side\n    passed: [false, false],\n    fatigue: false,\n    elementalHits: 0,\n    winner: null,\n    events: [],\n    matchup: settings.matchup || 'pvai',\n    totalActions: 0,\n    meta: { mulligans: [false, false] }\n  };\n  // 初始抽 5 张\n  [0, 1].forEach(side => draw(state, side, 5));\n  return state;\n}\n\nfunction pushEvent(state, ev) { state.events.push(ev); }\n\n// ---------------- 牌堆 ----------------\nfunction draw(state, side, n) {\n  const p = state.players[side];\n  for (let i = 0; i < n; i++) {\n    if (p.deck.length === 0) {\n      // 没有牌可抽：疲劳（这里以手牌不补充处理）\n      continue;\n    }\n    const idx = Math.floor(state.rng() * p.deck.length);\n    const card = p.deck.splice(idx, 1)[0];\n    p.hand.push({ id: card.id });\n  }\n}\n\nfunction discardFrom(state, side, handIndex) {\n  const p = state.players[side];\n  const [c] = p.hand.splice(handIndex, 1);\n  p.discards.push(c);\n  return c;\n}\n\n// ---------------- 体型 / 回合开始行动点 ----------------\nfunction smallCount(state, side) {\n  return state.players[side].team.filter(c => c.body === '小').length;\n}\n\nfunction computeAP(state, side) {\n  const p = state.players[side];\n  // 保留上一回合的“下一回合加成”（生物天赋）\n  const carrySummonAp = p.roundFlags ? (p.roundFlags.summonAp || 0) : 0;\n  const scenes = p.scenes.map(s => s.id);\n  let dice = 2;\n  if (scenes.includes('s_cesuo')) dice = 3;\n  const rolls = [];\n  for (let i = 0; i < dice; i++) rolls.push(1 + Math.floor(state.rng() * 6));\n  const calcAP = (a, b) => Math.floor(10 - Math.abs(a - b) / 2);\n  let ap = 0;\n  if (rolls.length === 2) {\n    ap = calcAP(rolls[0], rolls[1]);\n  } else {\n    // 厕所：掷 3 个，取最优一对\n    let best = -1;\n    for (let i = 0; i < 3; i++) for (let j = i + 1; j < 3; j++) {\n      const v = calcAP(rolls[i], rolls[j]);\n      if (v > best) best = v;\n    }\n    ap = best;\n  }\n  // 小体型 +1（最多 2 层）\n  ap += Math.min(smallCount(state, side), 2);\n  // 生物天赋：上回合结束记录的奇行种 -> 下一回合 AP+（最多+2）\n  ap += Math.min(carrySummonAp, 2);\n  // 场景/支援加成与减益\n  p.roundFlags = {};\n  // \\u5c0f\\u5356\\u90e8\n  const xmb = p.scenes.find(s => s.id === 's_xiaomaibu');\n  if (xmb) {\n    if (xmb.tick === 1) ap -= 1;       // 本回合 -1\n    else if (xmb.tick === 2) ap -= 1;  // 下一回合 -1（由 tick 记录）\n    else if (xmb.tick === 3) ap += 4;  // 再下一回合 +4\n  }\n  // 作业答案：持续2回合，每回合 +4\n  const zy = p.supports.find(s => s.id === 'sup_zuoye' && s.dur > 0);\n  if (zy) ap += 4;\n  // 钢笔/数字奥法之卷等在“回合开始前”结算（见 startRound）\n  state.players[side].computedAP = Math.max(0, ap);\n  return { ap: Math.max(0, ap), rolls };\n}\n\n// ---------------- 充能 ----------------\nfunction addCharge(char, n, cap) {\n  char.charge = Math.min(cap || char.chargeNeed || 3, char.charge + (n || 1));\n}\n\nfunction chargeNeed(state, side, char) {\n  const need = (char.talent === 't_shuxue') ? 2 : (char.chargeNeed || 3);\n  return need;\n}\n\n// ---------------- 元素附着 & 反应 ----------------\n// 返回 { reacted, type }, 并修改目标附着\nfunction applyElement(state, target, element, sourceInfo) {\n  if (element === '物理') return { reacted: false };\n  const current = target.attached;\n  if (!current) { target.attached = element; return { reacted: false }; }\n  if (current === element) {\n    if (element === '理') {\n      // 应激反应\n      target.attached = null;              // 反应后元素附着被消耗\n      target.buffs.push({ key: 'reactive', dur: 1, val: 2 });   // next hit +2 & 移除\n      target.status.cannotActRound = state.round;\n      return { reacted: true, type: '应激' };\n    } else {\n      // 失忆反应：流血 dot\n      addBleed(target, 2, 2);\n      target.attached = null;\n      return { reacted: true, type: '失忆' };\n    }\n  } else {\n    // 中和反应 文+理\n    target.attached = null;\n    return { reacted: true, type: '中和' };\n  }\n}\n\nfunction addBleed(target, dmg, dur) {\n  let b = target.buffs.find(x => x.key === 'bleed');\n  if (b) { b.dur = dur; b.dmg = dmg; b.n = (b.n || 0) + 0; }  // 不叠加，刷新持续\n  else target.buffs.push({ key: 'bleed', dur, dmg });\n}\n\nfunction addVulnerable(t, pct, dur) {\n  t.buffs.push({ key: 'vulnerable', val: pct, dur });\n}\n\n// 易伤百分比总和（含持续性易伤与“仅限1次”的一次性易伤）\nfunction vulnPct(t) {\n  return t.buffs.filter(b => (b.key === 'vulnerable' || b.key === 'vulnerableOne')\n      && (b.dur == null || b.dur > 0) && (b.uses == null || b.uses > 0))\n    .reduce((s, b) => s + (b.val || 0), 0);\n}\n\n// ---------------- 伤害结算 ----------------\n// opt: { amount, element, piercing, targetChar, sourceChar, sourceSide, kind, isAttack,\n//        coAttackDmg, summonDmg, ignoreCamo }\nfunction dealDamage(state, side, sourceChar, targetChar, opt) {\n  const events = [];\n  if (!targetChar) return events;\n  let amount = opt.amount || 0;\n  if (amount <= 0) return events;\n  const element = opt.element || '物理';\n  const sourceP = state.players[side];\n  const targetP = state.players[1 - side];\n  const targetIsActive = targetP.team[targetP.activeIndex] === targetChar;\n\n  // —— 迷彩：敌方行动无法选中（召唤物/后台穿透不受影响）\n  const isCamo = targetChar.buffs.some(b => b.key === 'camo');\n  if (!opt.ignoreCamo && isCamo && !opt.piercing && opt.kind !== 'coattack') {\n    events.push({ text: `${targetChar.name} 处于迷彩状态，本次攻击未能命中` });\n    return events;\n  }\n\n  // 元素反应\n  let reactedInfo = { reacted: false };\n  if (!opt.noReact && element !== '物理') {\n    reactedInfo = applyElement(state, targetChar, element, sourceChar);\n    // 中和反应：+2 伤害，给打出该反应的角色 +1 充能（每回合限1次）\n    if (reactedInfo.type === '中和') {\n      amount += 2;\n      if (!state.players[side].roundFlags.zhongheUsed || state.players[side].roundFlags.zhongheUsed < state.round) {\n        state.players[side].roundFlags.zhongheUsed = state.round;\n        addCharge(sourceChar, 1);\n        events.push({ text: `中和反应！伤害+2，${sourceChar.name} 充能+1` });\n      } else {\n        events.push({ text: `中和反应！伤害+2` });\n      }\n    } else if (reactedInfo.type === '应激') {\n      events.push({ text: `应激反应！${targetChar.name} 本回合无法行动，下次攻击伤害+2` });\n    } else if (reactedInfo.type === '失忆') {\n      events.push({ text: `失忆反应！${targetChar.name} 获得流血（每回合-2，持续2回合）` });\n    }\n  }\n\n  // —— 物理·电压 plus ultra 天赋：物理触发元素反应时，该次伤害+1并造成1点后台穿透且行动点+1（每回合限1次）\n  if (reactedInfo.reacted && sourceChar && sourceChar.id === 'wuli' && sourceChar.talent === 't_wuli') {\n    if (!state.players[side].roundFlags.wuliTalent || state.players[side].roundFlags.wuliTalent !== state.round) {\n      state.players[side].roundFlags.wuliTalent = state.round;\n      amount += 1;\n      events.push({ text: `物理天赋触发：本次伤害+1` });\n      const targetTeam = targetP.team;\n      const others = targetTeam.filter(c => c !== targetChar && c.hp > 0);\n      if (others.length) {\n        const back = others[Math.floor(state.rng() * others.length)];\n        dealDamage(state, side, sourceChar, back, { amount: 1, element: element, piercing: true, kind: 'pierce' });\n      }\n      state.players[side].ap = Math.max(0, state.players[side].ap + 1);\n    }\n  }\n\n  // —— 共鸣牌加成（大/小）：在产生元素反应时生效\n  if (reactedInfo.reacted && sourceChar) {\n    const daTalent = sourceP.team.some(c => c.talent === 'r_da' || c.talent_hint === 'r_da');\n    const xiaoTalent = sourceP.team.some(c => c.talent === 'r_xiao' || c.talent_hint === 'r_xiao');\n    if (daTalent && sourceChar.body === '大') {\n      if (reactedInfo.type === '中和' && element === '理') amount += 2;\n      else if (reactedInfo.type === '中和' && element === '文') amount += 1;\n    }\n    if (xiaoTalent && sourceChar.body === '小') {\n      if (reactedInfo.type === '中和' && element === '理') amount += 1;\n      else if (reactedInfo.type === '中和' && element === '文') amount += 2;\n    }\n  }\n\n  // 装备武器：伤害 +1\n  let weaponBonus = 0;\n  if (sourceChar && sourceChar.weapon) {\n    const oncePerRound = ['w_caozhi', 'w_qianbi'].includes(sourceChar.weapon);\n    if (oncePerRound) {\n      if (state.players[side].weaponBonusUsed !== state.round) {\n        weaponBonus += 1;\n        state.players[side].weaponBonusUsed = state.round;\n      }\n    } else {\n      weaponBonus += 1;\n    }\n    // 寰宇支配之剑：低血时会变成 +3\n    if (sourceChar.weapon === 'w_huanyu' && sourceChar.hp < 8) weaponBonus = 3;\n  }\n  amount += weaponBonus;\n\n  // 政治·智慧之论：护盾量 50% 增伤\n  if (sourceChar && sourceChar.id === 'zhengzhi' && sourceChar.talent === 't_zhengzhi') {\n    amount += roundHalfUp(sourceChar.shield * 0.5);\n  }\n\n  // —— 料理 buff\n  const food = sourceChar ? sourceChar.buffs.find(b => b.key === 'foodAtkBoost') : null;\n  if (food) amount += food.val;\n  const xiantiao = sourceChar ? sourceChar.buffs.find(b => b.key === 'foodBurstBoost' && b.uses > 0) : null;\n  if (xiantiao && opt.kind === 'burst') { amount += 4; xiantiao.uses--; }\n\n  // —— 政治天赋：身上有护盾时受到的伤害-1（在最后统一扣减） —— 先记录\n  const polShieldSelf = targetChar && targetChar.id === 'zhengzhi' && targetChar.shield > 0;\n  const polAtkShield = sourceChar && sourceChar.id === 'zhengzhi' && sourceChar.shield > 0;\n\n  // —— 易伤（不作用于后台穿透）\n  let vulnMult = 1;\n  if (!opt.piercing) vulnMult = 1 + vulnPct(targetChar) / 100;\n  // 应激的 next-hit +2 标记\n  const reactive = targetChar.buffs.find(b => b.key === 'reactive' && b.dur > 0);\n  if (reactive && opt.kind !== 'bleed') {\n    amount += reactive.val;\n    reactive.dur = 0;                       // 应激加成仅下一次攻击生效\n  }\n\n  // 历史天赋：对处于护盾下的角色造成的伤害 +30%\n  if (sourceP.team.some(c => c.talent === 't_lishi') && targetChar.shield > 0) {\n    amount *= 1.3;\n  }\n\n  // 应用易伤倍率并四舍五入\n  if (vulnMult !== 1) amount = roundHalfUp(amount * vulnMult);\n\n  if (polAtkShield) amount -= 1;   // 政治（有护盾）造成的伤害-1\n  if (polShieldSelf && !opt.ignorePolSelf) amount -= 1;\n\n  // 料理 莲花酥：受到的伤害 -3\n  const lianhua = targetChar.buffs.find(b => b.key === 'foodShieldReduce' && b.uses > 0);\n  if (lianhua) { amount -= 3; lianhua.uses--; if (lianhua.uses <= 0) lianhua.dur = 0; }\n  // 巴豆：对方下次攻击造成伤害-1（作用于敌方攻击我方时）在敌方攻击时处理\n  const badouDef = targetChar.buffs.find(b => b.key === 'foodBadouDef' && (b.uses == null || b.uses > 0));\n  if (badouDef && opt.kind !== 'bleed') { amount -= 1; if (badouDef.uses != null) badouDef.uses--; }\n\n  amount = Math.max(0, amount);\n\n  // 一次性易伤（数学爆发150%、彻骨冰寒30%）：本次受击后立即到期\n  if (!opt.piercing) {\n    const v1 = targetChar.buffs.find(b => b.key === 'vulnerableOne');\n    if (v1) {\n      v1.uses = (v1.uses || 1) - 1;\n      if (v1.uses <= 0) {\n        const vi = targetChar.buffs.indexOf(v1);\n        if (vi >= 0) targetChar.buffs.splice(vi, 1);   // 立即到期并移除\n      }\n    }\n  }\n\n  // 寰宇支配之剑：装备者受到的伤害+1\n  if (targetChar && targetChar.weapon === 'w_huanyu' && targetChar.hp >= 8) amount += 1;\n  if (targetChar && targetChar.weapon === 'w_huanyu' && targetChar.hp < 8) amount -= 1;\n\n  // 护盾 & 护心铠 吸收\n  let taken = amount;\n  if (targetChar.shield > 0) {\n    const absorbed = Math.min(targetChar.shield, taken);\n    targetChar.shield -= absorbed;\n    taken -= absorbed;\n    events.push({ text: `${targetChar.name} 的护盾吸收了 ${absorbed} 点伤害` });\n    // \\u6b62\\u8840\\u6ef4\\u4f53\\u7684\\u62a4\\u76fe\\u53ef\\u6b63\\u5e38\\u62b5\\u6321\\u8fd9\\u4e2a\\u4f24\\u5bb3\n  }\n  // 护心铠：受到3点以上伤害时抵消2点（可用2次）\n  const hxk = targetChar.buffs.find(b => b.key === 'huxinkai' && b.uses > 0);\n  if (hxk && taken > 3) {\n    const reduced = Math.min(2, taken);\n    taken -= reduced;\n    hxk.uses--;\n    events.push({ text: `护心铠抵消 ${reduced} 点伤害（剩余 ${hxk.uses} 次）` });\n  }\n\n  if (taken > 0) {\n    targetChar.hp = Math.max(0, targetChar.hp - taken);\n    events.push({ text: `${sourceChar ? sourceChar.name : '敌方'} 对 ${targetChar.name} 造成 ${taken} 点${element}伤害` });\n    const tp = state.players[1 - side];\n    const ti = tp.team.indexOf(targetChar);\n    events.push({\n      type: 'damage', sourceSide: side, targetSide: 1 - side, targetIndex: ti,\n      amount: taken, element, kind: opt.kind || 'normal'\n    });\n    // 赌徒的胸花：对方角色死亡时\n    if (targetChar.hp <= 0 && targetP.team.includes(targetChar)) {\n      targetP.stats.kills++;\n      targetP.roundFlags.thisRoundDied = true;\n      events.push({ text: `${targetChar.name} 被击败！` });\n      if (sourceP.team.some(c => c.artifact === 'a_dutu')) {\n        sourceP.ap += 3;\n        addCharge(sourceP.team[sourceP.activeIndex], 1);\n        events.push({ text: `赌徒的胸花触发：行动点+3，充能+1` });\n      }\n      // 检查全灭\n      if (targetP.team.every(c => c.hp <= 0)) {\n        state.phase = 'finished';\n        state.winner = side;\n      }\n    }\n  }\n\n  // 参量变质仪：双方共造成 5 次元素伤害后，持有者行动点 +4 并弃置\n  if (taken > 0 && element !== '物理') {\n    state.elementalHits = (state.elementalHits || 0) + 1;\n    if (state.elementalHits >= 5) {\n      [0, 1].forEach(sd => {\n        const pl = state.players[sd];\n        const idx = pl.supports.findIndex(s => s.id === 'sup_canliang');\n        if (idx >= 0) {\n          pl.ap += 4;\n          pl.supports.splice(idx, 1);\n          events.push({ text: `${pl.name} 的参量变质仪触发：行动点+4` });\n        }\n      });\n    }\n  }\n\n  // 计算机反弹\n  if (targetChar && targetChar.weapon === 'w_jisuanji' && opt.kind !== 'coattack' && opt.kind !== 'bleed') {\n    if (targetChar.hp > 0 && taken > 0) {\n      const reflect = Math.round(taken * 0.75);\n      if (sourceChar) {\n        sourceChar.hp = Math.max(0, sourceChar.hp - reflect);\n        events.push({ text: `计算机反弹 ${reflect} 点伤害` });\n      }\n      const heal = Math.round(taken * 0.5);\n      targetChar.hp = Math.min(targetChar.maxHp, targetChar.hp + heal);\n      events.push({ text: `计算机回复 ${heal} 点血量` });\n    }\n  }\n\n  return events;\n}\n\nfunction addShield(char, layers) {\n  char.shield = Math.min(4, char.shield + layers);\n}\n\nfunction healChar(char, amount) {\n  const before = char.hp;\n  char.hp = Math.min(char.maxHp, char.hp + amount);\n  return Math.min(amount, char.maxHp - before);\n}\n\nfunction healTeam(state, side, amount) {\n  const p = state.players[side];\n  let healed = 0;\n  p.team.forEach(c => { if (c.hp > 0) healed += healChar(c, amount); });\n  return healed;\n}\n\n// ---------------- 费用计算 ----------------\nfunction fatiguePenalty(state) { return state.fatigue ? 1 : 0; }\n\nfunction activeChar(p) { return p.team[p.activeIndex]; }\n\nfunction effCost(state, side, base, kind, char, isGangbiTriggered) {\n  let c = base + fatiguePenalty(state);\n  const p = state.players[side];\n  const ch = char || activeChar(p);\n  // 环保之心：攻击少1（每回合2次）\n  if (ch && ch.artifact === 'a_huanbao' && (kind === 'normal' || kind === 'skill' || kind === 'burst')) {\n    if ((p.roundFlags.huanbaoCount || 0) < 2) { c -= 1; }\n  }\n  // 北地烟熏鸡：接下来3次普攻-1\n  if (kind === 'normal' && ch) {\n    const chicken = ch.buffs.find(b => b.key === 'foodChicken' && b.uses > 0);\n    if (chicken) { c -= 1; }\n  }\n  // 政治爆发期间：普攻-1\n  if (kind === 'normal' && ch && ch.buffs.some(b => b.key === 'politBuff' && b.dur > 0)) {\n    c -= 1;\n  }\n  // 语文天赋：偶数回合，若对方向语文施加过“问题未答出”，语文战技/爆发-1\n  if ((kind === 'skill' || kind === 'burst') && ch && ch.id === 'yuwen' && p.roundFlags.yuwenDiscount > 0) {\n    c -= 1;\n  }\n  // 小卖部：本回合行动点减益已计入 AP，不在此处\n  return Math.max(0, c);\n}\n\n// ---------------- 回合开始 ----------------\nfunction startRound(state) {\n  state.phase = 'round';\n  state.round++;\n  state.passed = [false, false];\n  state.earlyEndedBy = null;\n  state.events.push({ type: 'round', round: state.round });\n\n  // 疲劳判定\n  if (state.round >= 10) state.fatigue = true;\n  if (state.round >= 15 && !state.winner) {\n    // 15回合未分胜负\n    if (state.round >= 15) {\n      // 在第15回合开始时若仍未分出胜负，允许继续但记录；在第16回合结束强制平局（见 endRound）\n    }\n  }\n\n  // 确定本回合先手\n  if (state.round === 1) {\n    // 开局掷骰\n    const r0 = 1 + Math.floor(state.rng() * 6);\n    const r1 = 1 + Math.floor(state.rng() * 6);\n    let first = r0 <= r1 ? 0 : 1;\n    if (r0 === r1) first = state.rng() < 0.5 ? 0 : 1;\n    state.firstThisRound = first;\n  } else {\n    if (state.earlyEndedBy != null) state.firstThisRound = state.earlyEndedBy;\n    else state.firstThisRound = 1 - state.firstThisRound; // 交替\n  }\n  state.current = state.firstThisRound;\n\n  // 计算双方行动点\n  [0, 1].forEach(side => {\n    const r = computeAP(state, side);\n    state.players[side].ap = r.ap;\n    state.players[side].diceRolls = r.rolls;\n  });\n  state.events.push({\n    type: 'dice_round', round: state.round,\n    rolls0: state.players[0].diceRolls, rolls1: state.players[1].diceRolls,\n    ap0: state.players[0].ap, ap1: state.players[1].ap,\n    first: state.firstThisRound\n  });\n\n  // 生物天赋：回合结束给下回合的 AP 加成（在 computeAP 里通过 roundFlags.previousSummonAp 体现）\n  // 这里处理每回合开头的“场景/天赋”即时效果\n  [0, 1].forEach(side => applyRoundStartEffects(state, side));\n\n  // 生物·三人成虎：每个奇行种给己方出战角色1层护盾\n  [0, 1].forEach(side => {\n    const p = state.players[side];\n    if (p.team.some(c => c.talent === 't_shengwu')) {\n      const n = p.summons.filter(s => s.key === 'qixing').length;\n      if (n > 0) { addShield(activeChar(p), n); pushEvent(state, { text: `三人成虎：出战角色获得 ${n} 层护盾` }); }\n    }\n  });\n\n  state.events.push({ type: 'turn', player: state.current });\n}\n\nfunction applyRoundStartEffects(state, side) {\n  const p = state.players[side];\n  const ch = activeChar(p);\n  // 钢笔的回合前掷骰\n  const wearer = p.team.find(c => c.weapon === 'w_gangbi');\n  if (wearer) {\n    const me = 1 + Math.floor(state.rng() * 6);\n    const op = 1 + Math.floor(state.rng() * 6);\n    if (op > me) { addCharge(ch, 1); pushEvent(state, { text: `钢笔：对方掷 ${op} > 己方 ${me}，${ch.name} 充能+1` }); }\n    else if (op < me) { state.players[1 - side].ap = Math.max(0, state.players[1 - side].ap - 1); pushEvent(state, { text: `钢笔：己方 ${me} > 对方 ${op}，对方本回合行动点-1` }); }\n  }\n  // 数字奥法之卷：回合前四则运算（由前端提问触发，这里记录触发点）\n  const shuzi = p.team.find(c => c.weapon === 'w_shuzi');\n  if (shuzi) {\n    pushEvent(state, { type: 'quiz', text: `数字奥法之卷：请回答24×24以内四则运算题` });\n  }\n}\n\n// 更新“眩晕”等状态每回合结束递减在 endRound 处理\n\n// ---------------- 回合结束 ----------------\nfunction roundEndShift(state) {\n  // 结束回合后的统一顺序：食物效果 > 召唤物效果 > 各种回血buff > 消去到期buff&debuff > 抽牌\n  const ev = [];\n  // 1) 食物效果\n  [0, 1].forEach(side => {\n    const p = state.players[side];\n    p.team.forEach(ch => {\n      const boluo = ch.buffs.find(b => b.key === 'foodPizza' && b.dur > 0);\n      if (boluo) { healChar(ch, 1); ev.push({ text: `（${ch.name}）菠萝披萨回复1血` }); }\n    });\n  });\n  // 2) 召唤物效果\n  [0, 1].forEach(side => {\n    const p = state.players[side];\n    const yueye = p.supports.some(s => s.id === 'sup_yueye' && s.dur > 0);\n    const attackChars = [];\n    p.summons.forEach(sm => {\n      if (sm.endOfRound === false) { if (sm.dur != null) sm.dur--; return; }   // 协同攻击型召唤物不在回合结束造成伤害\n      if (sm.dur != null && sm.dur <= 0) return;\n      let dmg = sm.dmg + (yueye ? 1 : 0);\n      if (sm.uses != null && sm.uses <= 0) return;\n      let targetChar = activeChar(state.players[1 - side]);\n      const tgt = dealDamage(state, side, p.team[p.activeIndex], targetChar, {\n        amount: dmg, element: sm.el, kind: 'summon', fromSummon: true, ignoreCamo: true\n      });\n      ev.push(...tgt);\n      if (sm.uses != null) sm.uses--;\n      if (sm.dur != null) sm.dur--;\n    });\n    p.summons = p.summons.filter(s => (s.dur == null || s.dur > 0) && (s.uses == null || s.uses > 0));\n  });\n  // 流血 dot（失忆反应） 及 各类状态回合末\n  [0, 1].forEach(side => {\n    const p = state.players[side];\n    p.team.forEach(ch => {\n      const bleed = ch.buffs.find(b => b.key === 'bleed' && b.dur > 0);\n      if (bleed) {\n        dealDamage(state, side, null, ch, { amount: bleed.dmg, element: '物理', kind: 'bleed', noReact: true, ignorePolSelf: true });\n        ev.push({ text: `（${ch.name}）流血-${bleed.dmg}` });\n      }\n    });\n  });\n  // 3) 各种回血buff（大体型回血、教师办公室、教室等）\n  [0, 1].forEach(side => {\n    const p = state.players[side];\n    // 大体型：每回合结束后 +4（3次/局，可指定发动，这里自动在不满血时发动）\n    p.team.forEach(ch => {\n      if (ch.body === '大' && ch.hp > 0 && ch.hp < ch.maxHp && ch.bigHealUsed < 3) {\n        healChar(ch, 4); ch.bigHealUsed++;\n        ev.push({ text: `${ch.name}（大体型）回血+4` });\n      }\n    });\n    // 教师办公室：回复出战角色2\n    if (p.scenes.some(s => s.id === 's_bangong' && s.dur > 0)) { healChar(activeChar(p), 2); ev.push({ text: `教师办公室：出战角色+2血` }); }\n    // 教室：回复血量最低的后台角色2\n    if (p.scenes.some(s => s.id === 's_jiaoshi' && s.dur > 0)) {\n      const back = p.team.filter((c, i) => i !== p.activeIndex && c.hp > 0);\n      if (back.length) { const low = back.reduce((a, b) => a.hp <= b.hp ? a : b); healChar(low, 2); ev.push({ text: `教室：${low.name}+2血` }); }\n    }\n    // 小卖部的 tick 递增\n    const xmb = p.scenes.find(s => s.id === 's_xiaomaibu');\n    if (xmb) { xmb.tick = (xmb.tick || 0) + 1; if (xmb.tick >= 4) p.scenes = p.scenes.filter(s => s !== xmb); }\n  });\n  // 物理天赋：回合末移除自身附着元素\n  [0, 1].forEach(side => {\n    const p = state.players[side];\n    p.team.forEach(ch => { if (ch.id === 'wuli' && ch.hp > 0) ch.attached = null; });\n  });\n  // 4) 消去到期 buff/debuff\n  [0, 1].forEach(side => {\n    const p = state.players[side];\n    p.team.forEach(ch => {\n      ch.buffs = ch.buffs.filter(b => {\n        if (b.dur != null) b.dur--;\n        return (b.dur === null || b.dur > 0) && (b.uses == null || b.uses > 0);\n      });\n      if (ch.status.cannotActRound === state.round) delete ch.status.cannotActRound;\n    });\n  });\n  // 5) 抽牌\n  [0, 1].forEach(side => {\n    const p = state.players[side];\n    let n = 2;\n    if (p.scenes.some(s => s.id === 's_caochang' && s.dur > 0)) n += 2;\n    draw(state, side, n);\n    // 场景牌：所有限时场景每回合结束 -1 回合（操场额外 +2 抽牌已在上面生效）\n    p.scenes.forEach(s => { if (s.dur != null) s.dur--; });\n    p.scenes = p.scenes.filter(s => s.dur === null || s.dur > 0);\n    // 支援牌：限时支援每回合结束 -1；武器大师到期后解锁“武器牌免费”\n    p.supports.forEach(s => {\n      if (s.dur != null && s.dur > 0) {\n        s.dur--;\n        if (s.dur <= 0 && s.id === 'sup_shifu') p.weaponFree = true;\n      }\n    });\n    p.supports = p.supports.filter(s => s.dur === null || s.dur > 0);\n  });\n  // 生物天赋：回合结束计数奇行种 -> 下回合 AP & 先手\n  [0, 1].forEach(side => {\n    const p = state.players[side];\n    if (p.team.some(c => c.talent === 't_shengwu')) {\n      const n = p.summons.filter(s => s.key === 'qixing').length;\n      p.roundFlags.summonAp = Math.min(n * 1, 2);\n      if (n >= 3) state.firstNextRound = side; // 下回合一定先手\n    } else p.roundFlags.summonAp = 0;\n  });\n  // 场景牌与支援牌持续回合数递减已在上面处理\n  // 语文天赋：偶数回合结束提问（问答题）\n  [0, 1].forEach(side => {\n    const p = state.players[side];\n    if (state.round % 2 === 0 && p.team.some(c => c.id === 'yuwen')) {\n      // 双方都有语文则抵消\n      const opponentHas = state.players[1 - side].team.some(c => c.id === 'yuwen');\n      if (!opponentHas) pushEvent(state, { type: 'question', side, text: '语文天赋提问：请回答一个知识点（10秒）' });\n    }\n  });\n  state.events.push(...ev);\n  return ev;\n}\n\nfunction checkActiveHerd(state, side) {\n  // 先手标记：若状态设置过 firstNextRound 给对方，则替换\n  return state;\n}\n\n// ---------------- 行动可执行性 ----------------\nfunction canAct(state, side) {\n  if (state.phase !== 'round') return false;\n  if (state.current !== side) return false;\n  if (state.passed && state.passed[side]) return false;\n  const p = state.players[side];\n  const ch = activeChar(p);\n  if (ch.hp <= 0) return false;\n  // 应激：该回合无法行动\n  if (ch.status.cannotActRound === state.round) return false;\n  return true;\n}\n\nfunction getValidActions(state, side) {\n  const p = state.players[side];\n  const res = [];\n  if (state.phase !== 'round' || state.current !== side) return res;\n  if (state.passed && state.passed[side]) return res;\n  const ch = activeChar(p);\n  const ap = p.ap;\n  const stunned = ch.status.cannotActRound === state.round || ch.buffs.some(b => b.key === 'cheguStun' && b.dur > 0);\n\n  if (ch.hp > 0 && !stunned) {\n    // 普攻\n    const ca = effCost(state, side, ch.a.cost, 'normal', ch);\n    if (ap >= ca) res.push({ type: 'attack', cost: ca, active: ch });\n    // 战技\n    const cs = effCost(state, side, ch.s.cost, 'skill', ch);\n    if (ap >= cs) res.push({ type: 'skill', cost: cs, active: ch });\n    if (ch.id === 'shengwu') {\n      const cs2 = effCost(state, side, ch.s2.cost, 'skill', ch);\n      if (ap >= cs2) res.push({ type: 'skill2', cost: cs2, active: ch });\n    }\n    // 爆发\n    const need = chargeNeed(state, side, ch);\n    if (ch.charge >= need) {\n      const cb = effCost(state, side, ch.b.cost, 'burst', ch);\n      if (ap >= cb) res.push({ type: 'burst', cost: cb, active: ch });\n    }\n  }\n  // 切人\n  if (!stunned || true) {\n    p.team.forEach((c, i) => {\n      if (i !== p.activeIndex && c.hp > 0) {\n        const cs = effCost(state, side, switchCost(state, side), 'switch');\n        if (ap >= cs) res.push({ type: 'switch', index: i, cost: cs });\n      }\n    });\n  }\n  // 调和（弃牌换行动点）\n  p.hand.forEach((card, hi) => {\n    const meta = CARD[card.id];\n    res.push({ type: 'tune', handIndex: hi, gain: Math.min((meta ? meta.cost : 1) + 1, 3) });\n  });\n  // 打牌\n  p.hand.forEach((card, hi) => {\n    if (canPlayCard(state, side, hi)) {\n      const meta = CARD[card.id];\n      const cost = cardCost(state, side, meta);\n      if (ap >= cost) res.push({ type: 'card', handIndex: hi, cost, meta });\n    }\n  });\n  // 结束（提前结束 -> 下回合先手）\n  res.push({ type: 'pass' });\n  return res;\n}\n\nfunction switchCost(state, side) {\n  const p = state.players[side];\n  let c = 1;\n  const yiwu = p.scenes.find(s => s.id === 's_yiwushi');\n  if (yiwu && !p.roundFlags.yiwuUsed) { c -= 1; }\n  return Math.max(0, c);\n}\n\nfunction canPlayCard(state, side, handIndex) {\n  const p = state.players[side];\n  const card = p.hand[handIndex];\n  if (!card) return false;\n  const meta = CARD[card.id];\n  if (!meta) return false;\n  // 共鸣：队伍中有2名或更多该属性\n  if (meta.type === 'resonance') {\n    if (meta.el === '文' || meta.el === '理') {\n      if (p.team.filter(c => c.element === meta.el).length < 2) return false;\n    } else if (card.id === 'r_da') {\n      if (p.team.filter(c => c.body === '大').length < 2) return false;\n    } else if (card.id === 'r_xiao') {\n      if (p.team.filter(c => c.body === '小').length < 2) return false;\n    }\n  }\n  // 天赋牌：仅对应角色为出战角色时可打出\n  if (meta.type === 'talent' && meta.char) {\n    if (activeChar(p).id !== meta.char) return false;\n  }\n  // 武器/饰品：仅限对应属性 + 每个角色只装1张\n  if (meta.type === 'weapon' || meta.type === 'artifact') {\n    const candidate = activeChar(p);\n    if (meta.el && candidate.element !== meta.el) return false;\n  }\n  // 东搬西补：后台角色无充能则无法打出\n  if (card.id === 'sup_dongban') {\n    const back = p.team.filter((c, i) => i !== p.activeIndex && c.hp > 0 && c.charge > 0);\n    if (!back.length) return false;\n  }\n  if (card.id === 'sup_wohai') {\n    if (!p.roundFlags.thisRoundDied) return false;\n  }\n  // 料理：每回合每位角色只能食用1次料理\n  if (meta.type === 'food') {\n    // 这里以出战角色是否已在本回合吃过料理判断\n    if (p.roundFlags.foodEatenThisRound) return false;\n  }\n  // 武器大师：打出武器牌无需行动点（仅限1次）\n  return true;\n}\n\n// ---------------- 行动结算 ----------------\nfunction endActionTurn(state, side) {\n  stepTurn(state, side, false);\n}\n\nfunction stepTurn(state, side, viaPass) {\n  // 眩晕强制切换：作用于本行动轮结束方（被眩晕者）\n  const ps = state.players[side];\n  if (activeChar(ps).buffs.some(b => b.key === 'stun' && b.dur > 0)) {\n    forceSwitch(state, side, activeChar(ps).status.stunCustom || 'left');\n  }\n  if (viaPass) {\n    state.passed[side] = true;\n    if (state.earlyEndedBy == null) state.earlyEndedBy = side;\n    if (state.passed[0] && state.passed[1]) { finalizeRound(state); return; }\n    state.current = 1 - side;\n  } else {\n    // 作战行动：结束你的行动轮，把行动权交给对方；若对方已结束，则你继续\n    if (!state.passed[1 - side]) state.current = 1 - side;\n  }\n  state.events.push({ type: 'turn', player: state.current });\n}\n\nfunction forceSwitch(state, side, dir) {\n  const p = state.players[side];\n  const n = p.team.length;\n  let idx = p.activeIndex;\n  if (dir === 'left') idx = (idx + 1) % n;\n  else if (dir === 'right') idx = (idx - 1 + n) % n;\n  // 找下一个存活角色\n  let guard = 0;\n  while (p.team[idx].hp <= 0 && guard < n) { idx = (idx + (dir === 'left' ? 1 : -1) + n) % n; guard++; }\n  p.activeIndex = idx;\n  pushEvent(state, { text: `${p.name} 被强制切换为 ${activeChar(p).name}` });\n}\n\nfunction finalizeRound(state) {\n  // 处理生物天赋下回合先手\n  if (state.firstNextRound != null) {\n    state.earlyEndedBy = state.firstNextRound;\n    state.firstNextRound = null;\n  }\n  roundEndShift(state);\n  state.events.push({ type: 'roundEnd' });\n  // 检查有没有被打到全灭\n  if ([0, 1].some(s => state.players[s].team.every(c => c.hp <= 0))) {\n    state.phase = 'finished';\n    state.winner = state.players[0].team.every(c => c.hp <= 0) ? 1 : 0;\n    pushEvent(state, { type: 'winner', winner: state.winner });\n    return;\n  }\n  // 疲劳触发：超10回合\n  if (state.round >= 10) state.fatigue = true;\n  if (state.round >= 15) {\n    state.phase = 'finished';\n    state.winner = -1; // 平局\n    pushEvent(state, { type: 'winner', winner: -1, draw: true });\n    return;\n  }\n  startRound(state);\n}\n\nfunction resolveAction(state, side, action) {\n  const p = state.players[side];\n  if (state.phase !== 'round' || state.current !== side) return;\n  if (action.type === 'pass') {\n    stepTurn(state, side, true);\n    return;\n  }\n  if (action.type === 'switch') {\n    const cost = effCost(state, side, switchCost(state, side), 'switch');\n    if (p.ap < cost) return;\n    p.ap -= cost;\n    p.roundFlags.yiwuUsed = true;\n    const ch = activeChar(p);\n    // 英语/黑笔/数学天赋等快速切换\n    const fast = isFastSwitch(state, side);\n    // 数学天赋：每回合一次\n    if (ch.id === 'shuxue' && !p.roundFlags.shuxueFast && ch.talent == null && p.team.some(c => c.talent === 't_shuxue')) {}\n    const fromChar = activeChar(p);\n    p.activeIndex = action.index;\n    pushEvent(state, { text: `${p.name} 切换为 ${activeChar(p).name}` });\n    if (fast) {\n      // 快速行动：不结束行动轮\n      return;\n    }\n    endActionTurn(state, side);\n    return;\n  }\n  if (action.type === 'attack' || action.type === 'skill' || action.type === 'skill2' || action.type === 'burst') {\n    performAbility(state, side, action);\n    return;\n  }\n  if (action.type === 'tune') {\n    const card = p.hand[action.handIndex];\n    const meta = CARD[card.id];\n    const gain = Math.min((meta ? meta.cost : 1) + 1, 3);\n    discardFrom(state, side, action.handIndex);\n    p.ap += gain;\n    pushEvent(state, { text: `${p.name} 调和，弃置 ${meta ? meta.name : '手牌'}，获得 ${gain} 行动点` });\n    return; // 快速\n  }\n  if (action.type === 'card') {\n    playCard(state, side, action.handIndex, action.targetChar, action.targetSummon, action.targetIndex);\n    return;\n  }\n}\n\nfunction isFastSwitch(state, side) {\n  const p = state.players[side];\n  const ch = activeChar(p);\n  // 数学天赋：从数学切出 -> 快速（每回合1次）\n  if (ch.id === 'shuxue' && !p.roundFlags.shuxueFast) {\n    // 需要数学已装备天赋？规则：“数学天赋：从数学切至其他角色时，视为快速行动”\n    // 这里要求装备了数学天赋牌才生效\n    if (p.team.some(c => c.talent === 't_shuxue')) { p.roundFlags.shuxueFast = true; return true; }\n  }\n  // 体育馆：每回合第1次切人快速\n  if (p.scenes.some(s => s.id === 's_tiyuguan' && s.dur != null && s.dur > 0) && !p.roundFlags.tiyuguanUsed) {\n    p.roundFlags.tiyuguanUsed = true; return true;\n  }\n  // 紧急战术\n  if (p.roundFlags.fastNextSwitch) { p.roundFlags.fastNextSwitch = false; return true; }\n  // 黑笔：元素反应后下次切人快速\n  if (p.roundFlags.fastNextSwitchHeibi) { p.roundFlags.fastNextSwitchHeibi = false; return true; }\n  return false;\n}\n\nfunction performAbility(state, side, action) {\n  const p = state.players[side];\n  const ch = activeChar(p);\n  const target = activeChar(state.players[1 - side]);\n  let kind, cost;\n  if (action.type === 'attack') { kind = 'normal'; cost = effCost(state, side, ch.a.cost, 'normal', ch); }\n  else if (action.type === 'skill') { kind = 'skill'; cost = effCost(state, side, ch.s.cost, 'skill', ch); }\n  else if (action.type === 'skill2') { kind = 'skill'; cost = effCost(state, side, ch.s2.cost, 'skill', ch); }\n  else { kind = 'burst'; cost = effCost(state, side, ch.b.cost, 'burst', ch); }\n  if (p.ap < cost) return;\n  p.ap -= cost;\n  const skillName = kind === 'normal' ? ch.a.name\n    : kind === 'skill' ? (action.type === 'skill2' ? ch.s2.name : ch.s.name)\n    : ch.b.name;\n  pushEvent(state, { type: 'skill_cast', side, charName: ch.name, skillName, kind });\n  // 环保之心计数\n  const huanbao = ch.artifact === 'a_huanbao' && (kind === 'normal' || kind === 'skill' || kind === 'burst');\n  if (huanbao) p.roundFlags.huanbaoCount = (p.roundFlags.huanbaoCount || 0) + 1;\n  // 北地烟熏鸡计数\n  if (kind === 'normal') {\n    const chicken = ch.buffs.find(b => b.key === 'foodChicken' && b.uses > 0);\n    if (chicken) chicken.uses--;\n  }\n\n  if (kind === 'normal') {\n    // 普攻：附魔决定元素\n    let el = ch.a.el;\n    const infuse = ch.buffs.find(b => b.key === 'infuse' && b.dur > 0);\n    if (infuse) el = infuse.el;\n    let dmg = ch.a.dmg;\n    if (infuse) dmg += 1; // 英语战技附魔伤害+1 / 政治爆发\n    if (ch.buffs.some(b => b.key === 'politBuff' && b.dur > 0)) { if (el === '物理') el = '文'; dmg += 1; }\n    // 政治爆发：普通攻击获得文元素附魔且伤害+1（已在上面）\n    // 巴豆：食用者下次攻击伤害+1\n    const badou = ch.buffs.find(b => b.key === 'foodBadouAtk' && (b.uses == null || b.uses > 0));\n    if (badou) { dmg += 1; if (badou.uses != null) badou.uses--; }\n    dealDamage(state, side, ch, target, { amount: dmg, element: el, kind: 'normal', isAttack: true });\n    // 物理·明朗之气协同\n    coAttack(state, side, target);\n    // 充能：普攻+1（呼吸累计）\n    addCharge(ch, 1);\n    // 生物召唤物协同在外层(roundEnd与coAttack)\n  } else if (kind === 'skill') {\n    const skill = (action.type === 'skill2') ? ch.s2 : ch.s;\n    // 战技伤害\n    let dmg = skill.dmg || 0;\n    if (skill.allEnemy) {\n      state.players[1 - side].team.forEach(c => { if (c.hp > 0) dealDamage(state, side, ch, c, { amount: dmg, element: skill.el || ch.element, kind: 'skill' }); });\n    } else {\n      if (dmg > 0) dealDamage(state, side, ch, target, { amount: dmg, element: skill.el || ch.element, kind: 'skill' });\n    }\n    // 召唤物\n    if (skill.summon) {\n      createSummonForSkill(state, side, ch, skill);\n    }\n    // 化学固有天赋：使用元素战技时自身 +2 血\n    if (ch.id === 'huaxue') { healChar(ch, 2); pushEvent(state, { text: `化学天赋：${ch.name} +2 血` }); }\n    // 自附着\n    if (skill.selfAttach) ch.attached = skill.selfAttach;\n    // 化学：每回合限1次（战技标记）\n    if (ch.id === 'huaxue' && !p.roundFlags.huaxueSkillUsed) p.roundFlags.huaxueSkillUsed = state.round;\n    else if (ch.id === 'huaxue' && p.roundFlags.huaxueSkillUsed === state.round) { /* 已用 */ }\n    // 政治战技：护心铠\n    if (skill.huxinkai) {\n      addShield(ch, 1);\n      ch.buffs.push({ key: 'huxinkai', uses: 2, dur: null });\n    }\n    if (skill.shield) addShield(ch, skill.shield);\n    // 物理战技自附着理元素\n    // 英语战技：附魔（不增加充能）\n    if (skill.infuse) {\n      ch.buffs = ch.buffs.filter(b => b.key !== 'infuse');\n      ch.buffs.push({ key: 'infuse', el: '文', dur: 2 });\n      // 英语天赋·幽夜净土：释放战技后获得2行动轮迷彩\n      if (ch.talent === 't_yingyu') ch.buffs.push({ key: 'camo', dur: 2, actionTurns: 2 });\n    } else {\n      addCharge(ch, 1);\n    }\n    // 历史天赋·巧思妙论等在回合内\n    coAttack(state, side, target);\n  } else { // burst\n    const need = chargeNeed(state, side, ch);\n    if (ch.charge < need) { p.ap += cost; pushEvent(state, { text: '充能不足' }); return; }\n    const b = ch.b;\n    // 造成伤害\n    let dmg = b.dmg || 0;\n    // 数学爆发：对对方角色施加150%易伤（不能使用1次）\n    if (ch.id === 'shuxue') {\n      target.buffs.push({ key: 'vulnerableOne', val: 150, uses: 1, dur: null });\n      pushEvent(state, { text: `${target.name} 获得150%易伤（仅1次）` });\n    } else {\n      if (b.allEnemy) {\n        state.players[1 - side].team.forEach(c => { if (c.hp > 0) dealDamage(state, side, ch, c, { amount: dmg, element: b.el || ch.element, kind: 'burst' }); });\n      } else if (dmg > 0) {\n        dealDamage(state, side, ch, target, { amount: dmg, element: b.el || ch.element, kind: 'burst' });\n      }\n      // 后台穿透\n      if (b.dmg2) {\n        // 语文：后台穿透不享受易伤\n        const back = state.players[1 - side].team.filter(c => c !== target && c.hp > 0);\n        if (back.length) {\n          const pick = back[0];\n          dealDamage(state, side, ch, pick, { amount: b.dmg2, element: b.el || ch.element, kind: 'pierce', piercing: true });\n        }\n        // 化学穿透\n      }\n      // 语文：施加易伤\n      if (ch.id === 'yuwen') { addVulnerable(target, 50, 2); pushEvent(state, { text: `${target.name} 获得50%易伤（2回合）` }); }\n      // 化学：眩晕\n      if (ch.id === 'huaxue' && b.stun) {\n        const stunDur = ch.talent === 't_huaxue' ? 3 : 2;\n        target.buffs.push({ key: 'stun', dur: stunDur });\n        if (ch.talent === 't_huaxue') target.status.stunCustom = 'left';\n        pushEvent(state, { text: `${target.name} 获得眩晕（${stunDur}回合，行动轮结束强制左移）` });\n      }\n      // 生物爆发\n      if (ch.id === 'shengwu') {\n        if (b.summonScale) {\n          const n = p.summons.filter(s => s.dur > 0).length;\n          dealDamage(state, side, ch, target, { amount: b.dmg + n, element: '理', kind: 'burst' });\n        }\n        if (b.healTeam) { healTeam(state, side, b.healTeam); pushEvent(state, { text: `全队回复${b.healTeam}血` }); }\n      }\n      // 历史爆发：+6穿透 +2护盾\n      if (ch.id === 'lishi') {\n        const back = state.players[1 - side].team.filter(c => c !== target && c.hp > 0);\n        if (back.length) dealDamage(state, side, ch, back[0], { amount: 6, element: '文', kind: 'pierce', piercing: true });\n        addShield(ch, 2);\n      }\n      // 政治爆发：普攻附魔+1&少1点行动点，持续2回合；期间护盾翻倍\n      if (ch.id === 'zhengzhi') {\n        ch.buffs.push({ key: 'politBuff', dur: 2 });\n        pushEvent(state, { text: ch.name + ' 获得政治强化状态（普攻附魔文且伤害+1、少消耗1点行动点）' });\n      }\n      // 地理爆发：雷暴之法在下方统一生成\n      // 物理爆发：明朗之气\n      if (ch.id === 'wuli' && b.summon) createSummonForSkill(state, side, ch, b);\n      // 英语爆发：迷彩\n      if (ch.id === 'yingyu' && b.camo) { ch.buffs.push({ key: 'camo', dur: 2 }); pushEvent(state, { text: `${ch.name} 获得迷彩（2回合）` }); }\n      // 地理爆发：雷暴之法\n      if (ch.id === 'dili' && b.leibao) {\n    p.summons.push(makeSummon(side, 'leibao', 3, '文', 2, null, ch, false));\n        pushEvent(state, { text: `生成「雷暴之法」：我方造成伤害时协同3点文伤害（2回合）` });\n      }\n    }\n    // 忍术作弊小抄：释放元素爆发时全队充能+1（整局2次）\n    if (ch.weapon === 'w_renshu') {\n      const rn = p.status_renshuUsed || 0;\n      if (rn < 2) { p.team.forEach(c => addCharge(c, 1)); p.status_renshuUsed = rn + 1; pushEvent(state, { text: `忍术作弊小抄：全队充能+1` }); }\n    }\n    // 消耗充能\n    ch.charge = Math.max(0, ch.charge - need);\n    coAttack(state, side, target);\n  }\n\n  // 战技/普攻 触发 雷暴之法 / 明朗之气 已在 coAttack 处理\n  // 元素反应产生时的英语/草稿纸/黑笔等触发\n  afterElementalEvent(state, side);\n  endActionTurn(state, side);\n}\n\nfunction coAttack(state, side, target) {\n  const p = state.players[side];\n  // 明朗之气：我方角色攻击时协同\n  const minglang = p.summons.find(s => s.key === 'minglang' && s.uses > 0);\n  if (minglang && target && target.hp > 0) {\n    dealDamage(state, side, activeChar(p), target, { amount: minglang.dmg, element: minglang.el, kind: 'coattack' });\n    minglang.uses--;\n  }\n  // 雷暴之法：我方造成伤害时协同\n  p.summons.forEach(sm => {\n    if (sm.key === 'leibao' && sm.dur > 0 && target && target.hp > 0) {\n      dealDamage(state, side, activeChar(p), target, { amount: sm.dmg, element: sm.el, kind: 'coattack' });\n    }\n  });\n}\n\nfunction createSummonForSkill(state, side, ch, skill) {\n  const p = state.players[side];\n  if (skill.summon === 'zuowen') {\n    // 语文·薪火不灭：作文无限+伤害2\n    const boosted = ch.talent === 't_yuwen';\n    pushSummon(p, makeSummon(side, 'zuowen', boosted ? 2 : 1, '文', boosted ? null : 2, null, ch));\n    pushEvent(state, { text: `生成「作文」（每回合结束造成${boosted ? 2 : 1}点文伤害${boosted ? '，持续为无限' : '，2回合'}）` });\n  } else if (skill.summon === 'jiexijihe') {\n    pushSummon(p, makeSummon(side, 'jiexijihe', 1, '理', 2, null, ch));\n    pushEvent(state, { text: `生成「解析几何」（每回合结束造成1点理伤害，2回合）` });\n  } else if (skill.summon === 'qixing') {\n    const n = skill.n || 1;\n    for (let i = 0; i < n; i++) pushSummon(p, makeSummon(side, 'qixing', 1, '理', skill.dur || 2, null, ch));\n    pushEvent(state, { text: `生成 ${n} 个「奇行种」（每回合结束造成1点理伤害${skill.dur ? '，' + skill.dur + '回合' : ''}）` });\n  } else if (skill.summon === 'minglang') {\n    pushSummon(p, makeSummon(side, 'minglang', 2, '理', null, 3, ch, false));\n    pushEvent(state, { text: `生成「明朗之气」（我方攻击协同2点理伤害，可用3次）` });\n  }\n}\n\nfunction pushSummon(p, sm) {\n  // 召唤物最多4个；重复刷新使用次数或持续回合\n  const dup = p.summons.find(s => s.key === sm.key && s.key !== 'qixing');\n  if (dup) { dup.dur = sm.dur != null ? sm.dur : dup.dur; dup.uses = sm.uses != null ? sm.uses : dup.uses; dup.dmg = sm.dmg; return; }\n  if (p.summons.length >= 4) return;\n  p.summons.push(sm);\n}\n\nfunction afterElementalEvent(state, side) {\n  const p = state.players[side];\n  const ch = activeChar(p);\n  // 英语天赋：产生元素反应行动点+1（每回合1次）\n  if (ch.id === 'yingyu' && !p.roundFlags.yingyuReaction && state.events.some(e => e.text && e.text.includes('反应'))) {\n    p.roundFlags.yingyuReaction = true;\n    p.ap += 1;\n    pushEvent(state, { text: `英语天赋：行动点+1` });\n  }\n  // 草稿纸：触发元素反应行动点+2（每回合1次）\n  if (ch.weapon === 'w_caogaozhi' && !p.roundFlags.caogaozhiReact && state.events.some(e => e.text && e.text.includes('反应'))) {\n    p.roundFlags.caogaozhiReact = true; p.ap += 2; pushEvent(state, { text: `草稿纸：行动点+2` });\n  }\n  // 黑笔：触发反应下次切人快速\n  if (ch.weapon === 'w_heibi' && !p.roundFlags.heibiReact && state.events.some(e => e.text && e.text.includes('反应'))) {\n    p.roundFlags.heibiReact = true; p.roundFlags.fastNextSwitchHeibi = true; pushEvent(state, { text: `黑笔：下次切人视为快速行动` });\n  }\n}\n\n// ---------------- 打牌 ----------------\nfunction cardCost(state, side, meta) {\n  const p = state.players[side];\n  let cost = meta.cost;\n  const ch = activeChar(p);\n  const isEquip = ['weapon', 'artifact', 'talent'].includes(meta.type);\n  const slot = meta.type === 'weapon' ? 'weapon' : meta.type === 'artifact' ? 'artifact' : 'talent';\n  const replacement = isEquip && ch && ch[slot] != null;\n  // 便携营养袋：打料理牌无需行动点（持续3回合）\n  if (meta.type === 'food' && p.supports.some(s => s.id === 'sup_yingyang' && s.dur > 0)) cost = 0;\n  // 武器大师：武器牌免费（仅限1次）\n  if (meta.type === 'weapon' && p.weaponFree && !replacement) cost = 0;\n  // 更换装备：额外消耗2行动点\n  if (replacement) cost += 2;\n  return Math.max(0, cost + fatiguePenalty(state));\n}\n\nfunction playCard(state, side, handIndex, targetChar, targetSummon, targetIndex) {\n  const p = state.players[side];\n  const card = p.hand[handIndex];\n  const meta = CARD[card.id];\n  if (!meta) return;\n  const cost = cardCost(state, side, meta);\n  if (p.ap < cost) return;\n  p.ap -= cost;\n  const ch = activeChar(p);\n  const endsTurn = cardEndsTurn(meta, state, side);\n  pushEvent(state, { type: 'card_play', side, cardId: meta.id });\n  if (meta.type === 'weapon' && p.weaponFree) p.weaponFree = false;\n  // 移除手牌\n  discardFrom(state, side, handIndex);\n\n  const enemyP = state.players[1 - side];\n  const target = targetChar || activeChar(enemyP);\n\n  switch (meta.type) {\n    case 'weapon':\n      ch.weapon = card.id;\n      pushEvent(state, { text: `${ch.name} 装备武器「${meta.name}」` });\n      break;\n    case 'artifact':\n      ch.artifact = card.id;\n      pushEvent(state, { text: `${ch.name} 装备饰品「${meta.name}」` });\n      break;\n    case 'talent':\n      ch.talent = card.id;\n      applyTalentOnPlay(state, side, card.id, ch);\n      pushEvent(state, { text: `${ch.name} 习得天赋「${meta.name}」` });\n      break;\n    case 'resonance':\n      applyResonance(state, side, card.id);\n      pushEvent(state, { text: `打出共鸣牌「${meta.name}」` });\n      break;\n    case 'scene':\n      applyScene(state, side, card.id);\n      pushEvent(state, { text: `打出场景牌「${meta.name}」` });\n      break;\n    case 'support':\n      playSupport(state, side, card.id, target, targetSummon, targetIndex);\n      break;\n    case 'food':\n      playFood(state, side, card.id, ch);\n      p.roundFlags.foodEatenThisRound = true;\n      pushEvent(state, { text: `${ch.name} 食用「${meta.name}」` });\n      break;\n  }\n  if (endsTurn) endActionTurn(state, side);\n}\n\nfunction cardEndsTurn(meta, state, side) {\n  // 装备牌：首次装备为快速行动；更换（该角色已装备同类牌）视为作战行动\n  if (['weapon', 'artifact', 'talent'].includes(meta.type)) {\n    const ch = activeChar(state.players[side]);\n    const slot = meta.type === 'weapon' ? 'weapon' : meta.type === 'artifact' ? 'artifact' : 'talent';\n    return !!(ch && ch[slot] != null);\n  }\n  // 其余手牌默认快速行动（若后续卡牌文本明确“视为作战行动”可在此补充）\n  return false;\n}\n\nfunction applyTalentOnPlay(state, side, id, ch) {\n  const p = state.players[side];\n  if (id === 't_yuwen') {\n    // 造成2点文伤害，生成强化作文\n    dealDamage(state, side, ch, activeChar(state.players[1 - side]), { amount: 2, element: '文', kind: 'card' });\n    pushSummon(p, makeSummon(side, 'zuowen', 2, '文', null, null, ch));\n  } else if (id === 't_dili' || id === 't_shuxue') {\n    // 数学速充/地理天赋：已在 chargeNeed / 法术里处理\n  }\n}\n\nfunction applyResonance(state, side, id) {\n  const p = state.players[side];\n  if (id === 'r_li' || id === 'r_wen') p.ap += 2;\n  // 大/小共鸣为被动，这里把效果标志挂到队伍（作为“天赋”存在）\n  if (id === 'r_da' || id === 'r_xiao') {\n    // 标记一枚共鸣（让对方可查询）\n    p.team.forEach(c => { if (!c.talent) c.talent_hint = id; });\n    pushEvent(state, { text: `共鸣「${id === 'r_da' ? '大' : '小'}·以${id === 'r_da' ? '大' : '小'}博${id === 'r_da' ? '小' : '大'}」生效` });\n  }\n}\n\nfunction applyScene(state, side, id) {\n  const p = state.players[side];\n  if (id === 's_jia') {\n    p.team.forEach(c => { c.maxHp += 1; c.hp = Math.min(c.maxHp, c.hp + 1); });\n  } else if (id === 's_xiaomaibu') {\n    p.scenes.push({ id: 's_xiaomaibu', dur: null, tick: 1 });\n  } else if (id === 's_cesuo') {\n    p.scenes.push({ id: 's_cesuo', dur: null });\n  } else if (id === 's_jiaoshi') { p.scenes.push({ id: 's_jiaoshi', dur: 2 }); }\n  else if (id === 's_bangong') { p.scenes.push({ id: 's_bangong', dur: 2 }); }\n  else if (id === 's_shitang') { p.scenes.push({ id: 's_shitang', dur: 3 }); }\n  else if (id === 's_tiyuguan') { p.scenes.push({ id: 's_tiyuguan', dur: null }); }\n  else if (id === 's_caochang') { p.scenes.push({ id: 's_caochang', dur: 2 }); }\n  else if (id === 's_yiwushi') { p.scenes.push({ id: 's_yiwushi', dur: null }); }\n}\n\nfunction playSupport(state, side, id, target, targetSummon, targetIndex) {\n  const p = state.players[side];\n  const enemyP = state.players[1 - side];\n  const ch = activeChar(p);\n  switch (id) {\n    case 'sup_zuoye': p.supports.push({ id, dur: 2 }); break;\n    case 'sup_yingyang': p.supports.push({ id, dur: 3 }); break;\n    case 'sup_canliang': p.supports.push({ id, dur: null, count: 0 }); break;\n    case 'sup_shifu': p.supports.push({ id, dur: 2 }); break;\n    case 'sup_niunai': {\n      // 清除一名角色附着元素与异常\n      const t = target || activeChar(enemyP);\n      t.attached = null;\n      t.buffs = t.buffs.filter(b => !['bleed', 'reactive', 'stun', 'cheguStun', 'vulnerableOne'].includes(b.key));\n      if (t.status) delete t.status.cannotActRound;\n      break;\n    }\n    case 'sup_yunchou': draw(state, side, 2); break;\n    case 'sup_jinji': p.roundFlags.fastNextSwitch = true; break;\n    case 'sup_chongdian': addCharge(ch, 1); break;\n    case 'sup_dongban': {\n      // 转移后台1点充能给出战角色\n      const back = p.team.filter((c, i) => i !== p.activeIndex && c.charge > 0);\n      if (back.length) { const from = back[0]; from.charge--; addCharge(ch, 1); }\n      break;\n    }\n    case 'sup_douzhuan': {\n      // 转移武器/饰品\n      const from = p.team[targetIndex != null ? targetIndex : 0];\n      // 交给前端处理目标，这里简化：从当前角色转移到另一个可装备角色\n      if (from && (from.weapon || from.artifact)) {\n        const to = p.team.find(c => c !== from && c.hp > 0);\n        if (to) { to.weapon = from.weapon || to.weapon; to.artifact = from.artifact || to.artifact; from.weapon = null; from.artifact = null; }\n      }\n      break;\n    }\n    case 'sup_yueye': {\n      // 消灭对方所有召唤物\n      enemyP.summons = [];\n      p.supports.push({ id, dur: 1 });\n      p.roundFlags.yueyeActive = true;\n      break;\n    }\n    case 'sup_jingzhun': p.roundFlags.freeTarget = true; break;\n    case 'sup_songni': {\n      // 选择对方1个召唤物消灭\n      if (targetSummon != null && enemyP.summons[targetSummon]) enemyP.summons.splice(targetSummon, 1);\n      else if (enemyP.summons.length) enemyP.summons.pop();\n      break;\n    }\n    case 'sup_zhaohuanshi': {\n      // 生成1个自选元素召唤物（前端选择 文/理）\n      const el = targetIndex === 1 ? '理' : '文';\n      pushSummon(p, makeSummon(side, 'zhaohuan', 2, el, 2, null, ch));\n      break;\n    }\n    case 'sup_wohai': {\n      p.ap += 2; addCharge(ch, 1);\n      break;\n    }\n    case 'sup_chegu': {\n      const t = target || activeChar(enemyP);\n      t.buffs.push({ key: 'cheguStun', dur: 1 });\n      t.buffs.push({ key: 'vulnerableOne', val: 30, uses: 1, dur: null });\n      break;\n    }\n  }\n}\n\nfunction playFood(state, side, id, ch) {\n  const p = state.players[side];\n  switch (id) {\n    case 'f_tiantianhua': healChar(ch, 1); break;\n    case 'f_yanxun': { const b = ch.buffs.find(x => x.key === 'foodChicken'); if (b) b.uses = Math.min(3, b.uses + 3); else ch.buffs.push({ key: 'foodChicken', uses: 3 }); break; }\n    case 'f_boluo': healChar(ch, 2); ch.buffs.push({ key: 'foodPizza', dur: 2 }); break;\n    case 'f_lianhua': ch.buffs.push({ key: 'foodShieldReduce', uses: 2 }); break;\n    case 'f_xiantiao': ch.buffs.push({ key: 'foodBurstBoost', uses: 1 }); break;\n    case 'f_jueyun': ch.hp = Math.max(0, ch.hp - 3); p.ap += 6; break;\n    case 'f_badou': ch.buffs.push({ key: 'foodBadouAtk', uses: 1 }); break;\n  }\n}\n\n// 暴击/易伤等“可用次数”的到期处理\nmodule.exports = {\n  createGame, startRound, resolveAction, getValidActions, canPlayCard, cardCost,\n  finalizeRound, roundEndShift, dealDamage, effCost, activeChar, pushEvent, createPlayer,\n  draw, discardFrom, chargeNeed\n};\n","./ai":"// 简单启发式 AI：选择行动\nconst { getValidActions } = require('./engine');\n\nfunction chooseAction(state, side) {\n  const va = getValidActions(state, side);\n  if (!va.length) return { type: 'pass' };\n  const p = state.players[side];\n  const ap = p.ap;\n  const by = t => va.filter(a => a.type === t);\n\n  // 1) 能放爆发且充能满 -> 放\n  const burst = by('burst');\n  if (burst.length) return burst[0];\n\n  // 2) 普攻\n  const attack = by('attack');\n  if (attack.length) return attack[0];\n\n  // 3) 元素战技\n  const skill = by('skill');\n  if (skill.length) return skill[0];\n\n  // 4) 低血量时切人\n  const ch = p.team[p.activeIndex];\n  if (ch && ch.hp <= 4) {\n    const sw = by('switch');\n    if (sw.length) return sw[0];\n  }\n\n  // 5) 打一张免费/便宜的效果牌（快速行动优先）\n  const cards = va.filter(a => a.type === 'card' && a.cost <= ap).sort((a, b) => a.cost - b.cost);\n  if (cards.length) {\n    // 优先选择会给行动点/抽牌/useful的\n    const useful = cards.find(c => ['r_wen', 'r_li', 'sup_yunchou', 'sup_chongdian', 'f_tiantianhua'].includes(c.meta.id));\n    return useful || cards[0];\n  }\n  // 6) 没有更有意义的行动 -> 结束\n  return { type: 'pass' };\n}\n\nmodule.exports = { chooseAction };\n","./deck":"// 卡组构建与预设\nconst CHARS = require('../data/characters');\nconst { cards } = require('../data/cards');\n\nconst ALL = cards.map(c => c.id);\n\n// AI 预设队伍（每次随机取一套）\nconst AI_PRESETS = [\n  ['yuwen', 'shuxue', 'yingyu'],      // 大三元\n  ['wuli', 'huaxue', 'shengwu'],      // 理·三小\n  ['zhengzhi', 'lishi', 'dili'],      // 文·三小\n  ['yuwen', 'yingyu', 'zhengzhi'],    // 文组\n  ['shuxue', 'wuli', 'huaxue'],       // 理组\n  ['yuwen', 'wuli', 'zhengzhi'],      // 均衡\n  ['shengwu', 'shuxue', 'huaxue'],    // 召唤流\n  ['lishi', 'dili', 'zhengzhi'],      // 护盾/辅助流\n  ['yuwen', 'shuxue', 'wuli'],        // 文理混编\n  ['yingyu', 'huaxue', 'shengwu'],    // 附魔/召唤\n  ['zhengzhi', 'shuxue', 'yingyu'],   // 护盾+爆发\n  ['dili', 'wuli', 'lishi'],          // 协同/治疗\n  ['yuwen', 'shengwu', 'dili'],       // 万能\n  ['shuxue', 'zhengzhi', 'huaxue']    // 控制/易伤\n];\n\n// 为给定队伍生成一套配套卡组（30 张，每种最多 2 张）\nfunction buildAIDeck(team, size) {\n  size = size || 30;\n  const teamSet = new Set(team);\n  const elems = team.map(id => CHARS[id].element);\n  const nWen = elems.filter(e => e === '文').length;\n  const nLi = elems.filter(e => e === '理').length;\n  const pool = cards.filter(c => {\n    if (c.type === 'talent' && c.char && !teamSet.has(c.char)) return false;\n    if (c.type === 'resonance') {\n      if (c.el === '文' && nWen < 2) return false;\n      if (c.el === '理' && nLi < 2) return false;\n      if (c.id === 'r_da' && team.filter(id => CHARS[id].body === '大').length < 2) return false;\n      if (c.id === 'r_xiao' && team.filter(id => CHARS[id].body === '小').length < 2) return false;\n    }\n    if (c.type === 'weapon' && c.el && !elems.includes(c.el)) return false;\n    return true;\n  });\n  const result = [];\n  const cnt = {};\n  // 先放入本队角色的天赋牌\n  pool.filter(c => c.type === 'talent').forEach(c => {\n    if (result.length < size) { result.push(c.id); cnt[c.id] = 1; }\n  });\n  const rest = pool.slice().sort((a, b) => a.cost - b.cost);\n  let guard = 0;\n  while (result.length < size && rest.length && guard < 800) {\n    guard++;\n    const c = rest[Math.floor(Math.random() * rest.length)];\n    if ((cnt[c.id] || 0) >= 2) continue;\n    cnt[c.id] = (cnt[c.id] || 0) + 1;\n    result.push(c.id);\n  }\n  let g2 = 0;\n  while (result.length < size && rest.length && g2 < 400) {\n    g2++;\n    result.push(rest[Math.floor(Math.random() * rest.length)].id);\n  }\n  return result.slice(0, size);\n}\n\n// 随机 AI 队伍：25% 完全随机三名角色，其余从预设中随机\nfunction randomAITeam() {\n  if (Math.random() < 0.25) {\n    const pool = Object.keys(CHARS);\n    for (let i = pool.length - 1; i > 0; i--) {\n      const j = Math.floor(Math.random() * (i + 1));\n      [pool[i], pool[j]] = [pool[j], pool[i]];\n    }\n    return pool.slice(0, 3);\n  }\n  return AI_PRESETS[Math.floor(Math.random() * AI_PRESETS.length)].slice();\n}\n\nfunction aiLoadout(size) {\n  const team = randomAITeam();\n  return { team, deck: buildAIDeck(team, size) };\n}\n\n// 推荐角色队伍\nconst TEAM_PRESETS = {\n  wen: ['yuwen', 'yingyu', 'zhengzhi'],\n  li: ['shuxue', 'wuli', 'huaxue'],\n  balanced: ['yuwen', 'wuli', 'zhengzhi'],\n  shengwu: ['shengwu', 'shuxue', 'huaxue']\n};\n\n// 各类型牌的权重（用于随机组一套功能牌）\nconst IDX = {};\ncards.forEach(c => { IDX[c.id] = c; });\n\nfunction buildDeck(theme, size) {\n  size = size || 30;\n  // 优先级：尽量包含多种牌且费用较低\n  const order = cards.map(c => c.id);\n  // 洗牌\n  for (let i = order.length - 1; i > 0; i--) {\n    const j = Math.floor(Math.random() * (i + 1));\n    [order[i], order[j]] = [order[j], order[i]];\n  }\n  const result = [];\n  const counts = {};\n  order.forEach(id => {\n    if (result.length >= size) return;\n    const meta = IDX[id];\n    if (meta.type === 'resonance') {\n      // 共鸣牌只在队伍有两名同属性时有用，这里限制数量\n      if (theme === 'wen' && id !== 'r_wen' && id !== 'r_da' && id !== 'r_xiao') return;\n      if (theme === 'li' && id !== 'r_li' && id !== 'r_da' && id !== 'r_xiao') return;\n    }\n    // 最多2张\n    if ((counts[id] || 0) >= 2) return;\n    counts[id] = (counts[id] || 0) + 1;\n    result.push(id);\n  });\n  // 不足则补足（不考虑重复上限）\n  let guard = 0;\n  while (result.length < size && guard < 200) {\n    const id = ALL[Math.floor(Math.random() * ALL.length)];\n    result.push(id);\n    guard++;\n  }\n  return result.slice(0, size);\n}\n\nfunction teamForTheme(theme) {\n  return (TEAM_PRESETS[theme] || TEAM_PRESETS.balanced).slice();\n}\n\nmodule.exports = { buildDeck, teamForTheme, TEAM_PRESETS, AI_PRESETS, buildAIDeck, randomAITeam, aiLoadout, ALL };\n"};
  var __cache = {};
  function __req(id) {
    if (__cache[id]) return __cache[id].exports;
    var m = { exports: {} };
    __cache[id] = m;
    var fn = new Function('module', 'exports', 'require', __src[id]);
    fn(m, m.exports, __req);
    return m.exports;
  }
  var chars = __req('../data/characters');
  var cardData = __req('../data/cards');
  window.XuekeEngine = __req('./engine');
  window.XuekeAI = __req('./ai');
  window.XuekeDeck = __req('./deck');
  window.XuekeData = {
    chars: chars,
    cards: cardData.cards,
    presets: {
      wen: ['yuwen', 'yingyu', 'zhengzhi'],
      li: ['shuxue', 'wuli', 'huaxue'],
      balanced: ['yuwen', 'wuli', 'zhengzhi'],
      shengwu: ['shengwu', 'shuxue', 'huaxue']
    }
  };
})();
